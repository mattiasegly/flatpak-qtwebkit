name: tcl
sources:
  - type: git
    url: https://github.com/tcltk/tcl.git
    tag: core-8-6-13
    commit: 338c6692672696a76b6cb4073820426406c6f3f9
    x-checker-data:
      type: git
      tag-pattern: core-(\d+-\d+-\d+)
buildsystem: simple
builddir: true
subdir: unix
build-commands:
  - ./configure --prefix=/app --enable-64bit
  - make
  - make install
post-install:
  - chmod 755 /app/lib/libtcl*.so
